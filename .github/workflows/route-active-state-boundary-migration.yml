name: Route Active State Boundary Migration

on:
  push:
    branches:
      - refactor/route-active-state-boundary

permissions:
  contents: write

jobs:
  migrate:
    name: Move external active reads to AppState once
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout migration branch
        uses: actions/checkout@v4
        with:
          ref: refactor/route-active-state-boundary
          fetch-depth: 0

      - name: Apply route active state boundary
        run: |
          node scripts/migrate-route-active-state-boundary.mjs
          node --check js/desktop-dashboard.js
          node --check js/enhancements.js
          node --check js/ride-tools.js
          node --check tests/e2e/route-active-state-boundary.spec.js
          sh -n scripts/check.sh
          git diff --check

      - name: Commit migration
        run: |
          if git diff --quiet -- js/desktop-dashboard.js js/enhancements.js js/ride-tools.js scripts/check.sh; then
            echo "Route active state boundary already applied."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add js/desktop-dashboard.js js/enhancements.js js/ride-tools.js scripts/check.sh
          git commit -m "refactor: read active route state from AppState"
          git push origin HEAD:refactor/route-active-state-boundary
