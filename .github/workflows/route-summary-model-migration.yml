name: Route Summary Model Migration

on:
  push:
    branches:
      - agent/portfolio-maintainability-pass
    paths:
      - scripts/migrate-route-summary-model.mjs
      - .github/workflows/route-summary-model-migration.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/portfolio-maintainability-pass

      - name: Apply route summary model migration
        run: node scripts/migrate-route-summary-model.mjs

      - name: Commit migration result
        run: |
          if git diff --quiet -- js/main-ui.js index.html; then
            echo "No route-summary migration changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add js/main-ui.js index.html
          git commit -m "refactor: delegate route summary presentation"
          git push origin HEAD:agent/portfolio-maintainability-pass
