name: Route Summary Wiring

on:
  push:
    branches:
      - refactor/wire-route-summary-model

permissions:
  contents: write

jobs:
  wiring:
    name: Wire route summary model once
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout wiring branch
        uses: actions/checkout@v4
        with:
          ref: refactor/wire-route-summary-model
          fetch-depth: 0

      - name: Apply route summary wiring
        run: |
          node scripts/wire-route-summary-runtime.mjs
          node --check js/main-ui.js
          node --check js/route-summary-model.js
          node --check sw.js
          git diff --check

      - name: Commit runtime wiring
        run: |
          if git diff --quiet -- index.html js/main-ui.js sw.js scripts/check.sh; then
            echo "Route summary wiring already applied."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html js/main-ui.js sw.js scripts/check.sh
          git commit -m "refactor: delegate route summary presentation"
          git push origin HEAD:refactor/wire-route-summary-model
